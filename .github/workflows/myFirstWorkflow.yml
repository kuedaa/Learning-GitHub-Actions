name: my-first-workflow
on:
  push: null
jobs:
  job1:
    name: JOB1
    runs-on: ubuntu-latest
    steps:
      - name: Step-1
        run: |
          pwd
          ls -ltr 
          hostname
      - name: Step-2 - Checkout
        uses: actions/checkout@v6.0.3
      - name: Step-3
        run: |
          pwd
          ls -ltr
          hostname
